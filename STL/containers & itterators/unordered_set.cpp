#include <iostream>
using namespace std;

    void explainUset(){
		unordered set<int> st;
		//loweer bound and upper bound function
		//does not work, rest all functions are same
		//as above it does store ant partivular order it has better complexity than set in most cases, except some when collision happens


	}